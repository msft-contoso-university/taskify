# Containers module

Scaffold only — no resources defined yet.

Will provision: Azure Container Registry and Container Apps Environment
(the shared hosting environment the application module's Container Apps run
in). Mirrors the source repo's `infrastructure/bicep/3-containers.bicep`
stage.
