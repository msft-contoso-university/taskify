# Foundation module

Scaffold only — no resources defined yet.

Will provision: resource group, virtual network/subnets, Key Vault, and
any shared naming/tag conventions used by the other modules. Mirrors the
source repo's `infrastructure/bicep/1-foundation.bicep` stage.
