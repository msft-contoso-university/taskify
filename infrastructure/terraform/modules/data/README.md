# Data module

Scaffold only — no resources defined yet.

Will provision: Azure Database for PostgreSQL Flexible Server, firewall
rules/private endpoint, and Key Vault secrets for connection credentials.
Mirrors the source repo's `infrastructure/bicep/2-data.bicep` stage.
